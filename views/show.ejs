<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Show</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">

</head>

<body>

  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">
        <%= ticket.name %>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Go Back</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/edit/<%=ticket._id%>">Edit</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- <ul>
    <li>@<%= ticket.price %>€</li>
    <li>Amount: <%= ticket.amount %>
    </li>
    <li>Category: <%= ticket.category %>
    </li>
    <li>Current Price: <%= ticket.currentPrice %>
    </li>
  </ul> -->

  
  <div class="container-xxl text-center">
    <table class="table table-light table-hover">
      <thead>
        <tr>
          <th scope="col">Name</th>
          <th scope="col">Amount</th>
          <th scope="col">Bought @</th>
          <th scope="col">Price on Steam</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th scope="row"><%= ticket.name %></th>
          <td><%= ticket.amount %></td>
          <td><%= ticket.price %></td>
          <td><%= ticket.currentPrice %></td>
        </tr>
      </tbody>
    </table>
  </div>
  
  <div class="container-xxl text-center">
    <table class="table table-light table-hover">
      <thead>
        <tr>
          <th scope="col">Investment</th>
          <th scope="col">Value</th>
          <th scope="col">After Tax</th>
          <th scope="col">ROI</th>
          <th scope="col">Gain</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th scope="row"><%= (ticket.price * ticket.amount).toFixed(2) %>€</th>
          <td><%= (ticket.currentPrice * ticket.amount).toFixed(2) %>€</td>
          <td><%= (ticket.currentPrice * ticket.amount * 0.75).toFixed(2) %>€ </td>
          <td><%= ((ticket.currentPrice * ticket.amount * 0.75).toFixed(2) / (ticket.price * ticket.amount) * 100).toFixed(2) %>% </td>
          <td><%= ((ticket.currentPrice * ticket.amount * 0.75).toFixed(2)  - ticket.price * ticket.amount).toFixed(2) %>€ </td>
        </tr>
      </tbody>
    </table>
    <form action="/<%=ticket._id%>?_method=DELETE" method="POST">
      <button class="btn btn-primary">Delete</button>
    </form>
  </div>
  
  

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3"
    crossorigin="anonymous"></script>

</body>

</html>